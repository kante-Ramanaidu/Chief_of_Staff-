# The Draft Desk — Python dependencies
# Install with: pip install -r requirements.txt

# Streamlit web UI
streamlit

# Gemini AI — two packages are used:
#   google-genai      → from google import genai  (new google-genai SDK)
#   google-generativeai → import google.generativeai as genai  (legacy SDK used by triage.py)
google-genai
google-generativeai

# Google OAuth + API clients
google-auth
google-auth-oauthlib
google-api-python-client

# Environment variable loading (.env file support)
python-dotenv
